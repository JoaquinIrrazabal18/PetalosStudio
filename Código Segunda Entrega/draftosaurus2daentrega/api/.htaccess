RewriteEngine On

# Si la petición no es a un archivo o directorio que ya existe...
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# ...redirige la petición al script index.php
RewriteRule ^(.*)$ index.php [QSA,L]